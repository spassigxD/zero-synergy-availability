/**
 * Vorlage für firebase-config.js
 *
 * 1. Firebase Console → Projekteinstellungen → Deine Apps → Web (</>)
 * 2. Nach "App registrieren" erscheint firebaseConfig { ... }
 * 3. Die 4 Werte unten eintragen, Datei als firebase-config.js speichern
 * 4. Nicht committen, solange noch Platzhalter drinstehen
 */
window.FIREBASE_CONFIG = {
  // firebaseConfig.apiKey
  apiKey: "HIER_API_KEY_EINFÜGEN",
  // firebaseConfig.authDomain
  authDomain: "HIER_PROJEKT_ID.firebaseapp.com",
  // firebaseConfig.databaseURL — muss Realtime Database sein (default-rtdb…)
  databaseURL: "https://HIER_PROJEKT_ID-default-rtdb.europe-west1.firebasedatabase.app",
  // firebaseConfig.projectId
  projectId: "HIER_PROJEKT_ID",
};
